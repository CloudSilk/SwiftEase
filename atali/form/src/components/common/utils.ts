import {
    AtaliButtonDesigner as AtaliButton,
    AtaliFormDrawerDesigner as AtaliFormDrawer, AtaliCommonContainer, AtaliSelect, AtaliTreeSelect,
    AtaliCascader, AtaliPopover, AtaliStatistic, AtaliTooltip, AtaliAlert, AtaliColorInput,
    AtaliCarousel,AtaliUnit,AtaliAvatar,AtaliImage,AtaliEditor, AtaliAutoComplete,AtaliSearch
} from './designer'

export const commonComponents = [
    AtaliButton, AtaliFormDrawer, AtaliCommonContainer, AtaliSelect, AtaliTreeSelect,
    AtaliCascader, AtaliPopover, AtaliStatistic, AtaliTooltip, AtaliAlert, AtaliColorInput,
    AtaliCarousel,AtaliUnit,AtaliAvatar,AtaliImage,AtaliEditor, AtaliAutoComplete,AtaliSearch
]

export const commonObject={
    AtaliButton, AtaliFormDrawer, AtaliCommonContainer, AtaliSelect, AtaliTreeSelect,
    AtaliCascader, AtaliPopover, AtaliStatistic, AtaliTooltip, AtaliAlert, AtaliColorInput,
    AtaliCarousel,AtaliUnit,AtaliAvatar,AtaliImage,AtaliEditor, AtaliAutoComplete,AtaliSearch
}